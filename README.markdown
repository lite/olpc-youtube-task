# api
 
    sinatra
    rspec

# shell 

    ruby olpc-tasks.rb 
    rake spec

# heroku

    heroku create olpc-tasks
    heroku addons:add shared-database:5mb
    git push heroku master

# ci
    travis-ci

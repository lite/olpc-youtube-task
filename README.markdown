# api
 
    sinatra
    rspec

# shell 

    ruby olpc-task.rb 
    rake spec

# heroku

    heroku create olpc-task
    heroku addons:add shared-database:5mb
    git push heroku master

# ci
    travis-ci
